# dotfiles

nah, who cares about windows d:

to initialize submodules: `git submodule update --init --depth=1`


another thing that's impacted by rootcerts is [uv](https://docs.astral.sh/uv/concepts/authentication/#custom-ca-certificates), need to document somehow...
